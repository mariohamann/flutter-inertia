name: flutter_inertia
description: "Inertia.js adapter for Flutter. Route Inertia.js requests to Dart handlers instead of an HTTP server."
version: 0.2.0
homepage: https://github.com/mariohamann/flutter-inertia
repository: https://github.com/mariohamann/flutter-inertia
issue_tracker: https://github.com/mariohamann/flutter-inertia/issues

topics:
  - inertia
  - webview
  - routing
  - web

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.12.0
  webview_flutter_wkwebview: ^3.25.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
