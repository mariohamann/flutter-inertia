# Build outputs — generated by `pnpm build` / `vite build`
assets/www/
example/assets/www/
packages/flutter-inertia-adapter/dist/

# Dependencies
node_modules/

# Flutter
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
*.iml
build/
pubspec.lock
# Keep example/pubspec.lock checked-in (required for reproducible example builds)
!example/pubspec.lock

# IDE
.vscode/

# macOS
.DS_Store

# Scratchpad / reference implementations
inspiration/
